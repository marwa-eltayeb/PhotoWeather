<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.main.MainActivity">

    <ImageView
        android:id="@+id/imgPhotoWeather"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/btnTakePhoto"
        android:layout_marginStart="@dimen/main_image_marginStart"
        android:layout_marginTop="@dimen/main_image_marginTop"
        android:layout_marginEnd="@dimen/main_image_marginEnd"
        android:layout_marginBottom="@dimen/main_image_marginBottom"
        android:contentDescription="@string/photo_weather"
        android:src="@drawable/image_preview" />

    <Button
        android:id="@+id/btnTakePhoto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/main_button_marginBottom"
        android:text="@string/take_photo"
        android:textAllCaps="false" />

    <ProgressBar
        android:id="@+id/loadingIndicator"
        style="@style/Widget.AppCompat.ProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="invisible" />
</RelativeLayout>